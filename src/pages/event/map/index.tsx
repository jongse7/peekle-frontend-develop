import { MapHeader, EventMapContainer } from '@/components';

const EventMapPage = () => {
  return (
    <section>
      <MapHeader />
      <EventMapContainer />
    </section>
  );
};

export default EventMapPage;
