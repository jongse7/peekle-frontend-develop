// import { useGetPermissions } from '@/hooks';

const AuthorizeRolePage = () => {
  // const { data: permissions } = useGetPermissions();
  // console.log(permissions);
  return <div>롤 부여</div>;
};

export default AuthorizeRolePage;
